# 项目记忆

## 功能更新交付约定

用户于 2026-09-08 再次明确确认：`CNQQC/xhs-mcp` 是用户自己的仓库，允许将本项目源码推送到该仓库；`newrepo` 是该已授权仓库的远程名称。

用户于 2026-09-08 明确授权：每个功能更新验证通过后，主动完成 Git commit、push，并部署到服务器，不停在本地修改完成，也无需逐次再次询问 push 或部署许可。此约定适用于当前及后续功能更新，除非用户另有指示。

- 先运行与改动相关的验证及项目必要检查；验证失败先修复，不发布未验证版本。
- 仅提交本次任务相关文件，保留用户其他未提交改动。功能使用独立分支，完成本地 review，并向个人仓库 `newrepo`（`CNQQC/xhs-mcp`）推送及创建 PR 供远程 review；基础分支为 `main`。不要推送到上游 `origin`（`xpzouying/xiaohongshu-mcp`）。
- push 成功后部署同一提交。当前服务器 SSH 别名 `aliyun`，项目目录 `/opt/xiaohongshu-mcp`，Compose 服务/容器 `xiaohongshu-mcp`。
- 当前应用是 Linux amd64 Go 二进制，宿主机 `app-patched` 挂载为容器 `/app/app`。部署前核实实际 Compose 配置；交叉编译、上传临时文件、备份当前二进制，再替换并重建或重启服务。保留现有环境变量、鉴权和 cookies，不将凭证输出或提交。
- 部署后验证服务健康以及更新功能（至少 MCP 工具注册/调用契约），核对运行版本。失败时恢复上一版本，说明失败原因。
- 完成时简要报告验证结果、提交/push 状态和部署验证结果；遇到真实权限或连接阻塞时明确说明，不把未完成的部署说成已完成。

部署背景与操作参考：`deploy/aliyun/README.md` 以及工作区父目录 `README.md`。历史文档可能过时，以服务器实际配置为准。
